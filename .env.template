# Ant Authentication Configuration

# [REQUIRED] Secret for JWT signing. Must be at least 32 characters long.
# Generate one with: openssl rand -base64 32
ANT_JWT_SECRET=your-32-character-secret-at-least

# [REQUIRED] Authorized username
ANT_AUTH_USER=admin

# [DEV ONLY] Authorized password. 
# Only allowed when NODE_ENV is not 'production'.
ANT_AUTH_PASSWORD=admin-password

# [PROD REQUIRED] Bcrypt hash of the password.
# Required when NODE_ENV is 'production'. 
# Use a tool or script to generate a bcrypt hash.
ANT_AUTH_PASSWORD_HASH=$2a$10$vI8tmv..somehash..

# [OPTIONAL] Token expiration time (e.g., 1h, 1d, 7d).
# Default: 1d in production, 7d in development.
ANT_TOKEN_TTL=1d
